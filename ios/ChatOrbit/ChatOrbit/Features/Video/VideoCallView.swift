import SwiftUI

struct LegacyVideoPlaceholder: View {
    var body: some View {
        Text("Video call UI has moved to SessionView.")
            .foregroundStyle(.secondary)
            .padding()
    }
}
